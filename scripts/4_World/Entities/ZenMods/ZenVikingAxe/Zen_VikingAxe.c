class Zen_VikingAxe extends Hatchet 
{
	/*protected void VikingAxeShowSimpleSelection(string selectionName, bool hide = false)
    {
        TStringArray selectionNames = new TStringArray;
        ConfigGetTextArray("simpleHiddenSelections", selectionNames);

		if (!selectionNames)
			return;

        int selectionId = selectionNames.Find(selectionName);
        SetSimpleHiddenSelectionState(selectionId, hide);
    }

	void Zen_VikingAxe()
	{
		VikingAxeShowSimpleSelection("backwards", 0);
		VikingAxeShowSimpleSelection("front", 1);
	}

	void SetAxeBackwards(bool b)
	{
		if (b)
		{
			VikingAxeShowSimpleSelection("backwards", 1);
			VikingAxeShowSimpleSelection("front", 0);
		}
		else 
		{
			VikingAxeShowSimpleSelection("backwards", 0);
			VikingAxeShowSimpleSelection("front", 1);
		}
	}*/
};