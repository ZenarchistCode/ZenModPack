modded class TrapBase 
{
	/*protected PlayerBase m_ZenModTrapperPlayer;
	
	PlayerBase ZenMod_GetPlayerTrapper()
	{
		return m_ZenModTrapperPlayer;
	}
	
	void SetZenMod_TrapperPlayer(PlayerBase pb)
	{
		m_ZenModTrapperPlayer = pb;
	}
	
	override void StartActivate(PlayerBase player)
	{
		super.StartActivate(player);

		if (GetGame().IsDedicatedServer())
		{
			SetZenMod_TrapperPlayer(player);
		}
	}*/
}